# Fixture for test/build-package.mjs — the smallest module the contract allows,
# plus one payload file. Packed by the system tar at test time, so the reader is
# exercised against a real tar program rather than fw.js writing for itself.
NAME=citest-mod
VERSION=0.0.1
DESCRIPTION="CI fixture module"
